import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    container: {
        flex: 1,
        padding: 10,
    },
    logoContainer: {
        position: 'absolute',
        top: 10,
        left: 10,
    },
    chatRoomContainer: {
        marginTop: 70,
    },
});

export default styles;