// app/_layout.tsx
import { Stack } from 'expo-router';
import React from 'react';

const Layout = () => {
  return (
    <Stack>
      {/* Stack.Screen은 파일 기반 라우팅에 의해 자동으로 처리됩니다. */}
    </Stack>
  );
};

export default Layout;
